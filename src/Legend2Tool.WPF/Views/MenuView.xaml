<UserControl x:Class="Legend2Tool.WPF.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Legend2Tool.WPF.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Legend2Tool.WPF.ViewModels"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             d:DataContext="{d:DesignInstance vm:MenuViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <StackPanel Grid.Column="0"
                    Height="28"
                    Margin="25,5,0,5"
                    Orientation="Horizontal">
            <Label Content="服务端目录：" />
            <TextBox Width="200"
                     Margin="5,0,0,0"
                     Text="{Binding ServerDirectory, UpdateSourceTrigger=PropertyChanged}" />
            <Button Margin="5,0,0,0"
                    Command="{Binding SetServerDirectoryCommand}"
                    Content="浏览" />
        </StackPanel>
        <StackPanel Grid.Column="0"
                    Height="28"
                    Margin="50,5,0,5"
                    Orientation="Horizontal">
            <Label Content="补丁目录：" />
            <TextBox Width="200"
                     Margin="5,0,0,0"
                     Text="{Binding PatchDirectory, UpdateSourceTrigger=PropertyChanged}" />
            <Button Margin="5,0,0,0"
                    Command="{Binding SetPatchDirectoryCommand}"
                    Content="浏览" />
        </StackPanel>
    </StackPanel>
</UserControl>
