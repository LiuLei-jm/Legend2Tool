<UserControl x:Class="Legend2Tool.WPF.Views.ScriptOptimizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Legend2Tool.WPF.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="clr-namespace:Legend2Tool.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:ScriptOptimizationViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
    <StackPanel>
        <Label Content="服务端目录：" />
        <TextBlock Text="{Binding ServerDirectory}" />
        <Label Content="补丁目录：" />
        <TextBlock Text="{Binding PatchDirectory}" />
    </StackPanel>
</UserControl>
