<UserControl
    x:Class="Legend2Tool.WPF.Views.DynamicMonsterSpawningView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:autogrid="clr-namespace:WpfAutoGrid;assembly=WpfAutoGrid.Core"
    xmlns:cv="clr-namespace:Legend2Tool.WPF.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:local="clr-namespace:Legend2Tool.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:Legend2Tool.WPF.ViewModels"
    d:Background="LightGray"
    d:DataContext="{d:DesignInstance Type=vm:DynamicMonsterSpawningViewModel}"
    d:DesignHeight="600"
    d:DesignWidth="1000"
    mc:Ignorable="d">
    <UserControl.Resources>
        <cv:MonsterCountToRedBrushConverter x:Key="MonsterCountToRedBrushConverter" />
    </UserControl.Resources>
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <TextBlock
                    Margin="10,0,0,0"
                    Foreground="Black"
                    Text="过滤设置(填入与Mongen.txt文件中吻合的数据，将忽略不会生成动态脚本" />
                <TextBlock
                    Margin="10,0,0,0"
                    Foreground="Black"
                    Text="每行填入一个名称，不要添加任何符号，多余的符号将无法命中" />
                <Separator />
                <autogrid:AutoGrid ColumnCount="5">
                    <autogrid:AutoGrid.Resources>
                        <Style TargetType="DockPanel">
                            <Setter Property="Height" Value="380" />
                            <Setter Property="Width" Value="110" />
                        </Style>
                        <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
                            <Setter Property="DockPanel.Dock" Value="Top" />
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="Foreground" Value="Black" />
                        </Style>
                        <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
                            <Setter Property="AcceptsReturn" Value="True" />
                            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
                            <Setter Property="HorizontalScrollBarVisibility" Value="Auto" />
                            <Setter Property="VerticalContentAlignment" Value="Top" />
                        </Style>
                    </autogrid:AutoGrid.Resources>
                    <DockPanel>
                        <TextBlock Text="地图代码" />
                        <TextBox Text="{Binding FilterMapCode, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                    <DockPanel Grid.Column="1">
                        <TextBlock Text="怪物名字" />
                        <TextBox Text="{Binding FilterMonName, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                    <DockPanel Grid.Column="2">
                        <TextBlock Text="怪物数量" />
                        <TextBox Text="{Binding FilterMonCount, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                    <DockPanel Grid.Column="3">
                        <TextBlock Text="刷新间隔" />
                        <TextBox Text="{Binding FilterInterval, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                    <DockPanel Grid.Column="4">
                        <TextBlock Text="名字颜色" />
                        <TextBox Text="{Binding FilterMonNameColor, UpdateSourceTrigger=PropertyChanged}" />
                    </DockPanel>
                </autogrid:AutoGrid>
            </StackPanel>

            <StackPanel>
                <autogrid:AutoGrid ColumnCount="2" RowCount="10">
                    <autogrid:AutoGrid.Resources>
                        <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="Margin" Value="5" />
                            <Setter Property="Foreground" Value="Black" />
                        </Style>
                        <Style BasedOn="{StaticResource {x:Type hc:NumericUpDown}}" TargetType="hc:NumericUpDown">
                            <Setter Property="Margin" Value="2" />
                        </Style>
                        <Style BasedOn="{StaticResource {x:Type CheckBox}}" TargetType="CheckBox">
                            <Setter Property="Margin" Value="5,8,5,8" />
                            <Setter Property="HorizontalAlignment" Value="Center" />
                        </Style>
                    </autogrid:AutoGrid.Resources>
                    <TextBlock Text="时间单位" />
                    <ComboBox
                        Margin="2"
                        SelectedValue="{Binding SelectedTimeUnit}"
                        SelectedValuePath="Content">
                        <ComboBoxItem Content="分" />
                        <ComboBoxItem Content="秒" />
                    </ComboBox>

                    <TextBlock Text="刷怪间隔" />
                    <hc:NumericUpDown
                        Maximum="60"
                        Minimum="0"
                        Value="{Binding RefreshMonInterval}" />

                    <TextBlock Text="清理怪物" />
                    <CheckBox IsChecked="{Binding IsClearMon}" />
                    <TextBlock Text="清怪间隔" />
                    <hc:NumericUpDown
                        Maximum="60"
                        Minimum="0"
                        Value="{Binding ClearMonInterval}" />

                    <TextBlock Text="刷怪倍数" />
                    <hc:NumericUpDown
                        Maximum="10"
                        Minimum="1"
                        Value="{Binding RefreshMonMultiplier}" />


                    <TextBlock Text="限制刷新时长" />
                    <CheckBox IsChecked="{Binding IsLimitRefreshInterval}" />

                    <TextBlock Text="单个最大刷新时长" />
                    <hc:NumericUpDown
                        Maximum="600"
                        Minimum="0"
                        Value="{Binding MaxRefreshInterval}" />

                    <TextBlock Text="单个怪物最大数量" />
                    <hc:NumericUpDown
                        Maximum="500"
                        Minimum="0"
                        Value="{Binding MaxRefreshCount}" />

                    <TextBlock Text="单个地图最大数量" />
                    <hc:NumericUpDown
                        Maximum="10000"
                        Minimum="0"
                        Value="{Binding MaxMonstersPerMap}" />

                    <TextBlock Text="注释Mongen" />
                    <CheckBox IsChecked="{Binding IsCommentMongen}" />

                </autogrid:AutoGrid>

                <Button
                    Width="100"
                    Command="{Binding SaveConfigCommand}"
                    Content="保存配置" />
            </StackPanel>

            <StackPanel>
                <autogrid:AutoGrid ColumnCount="2" RowCount="10">
                    <autogrid:AutoGrid.Resources>
                        <Style BasedOn="{StaticResource {x:Type TextBlock}}" TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="Margin" Value="5" />
                            <Setter Property="Foreground" Value="Black" />
                        </Style>
                        <Style BasedOn="{StaticResource {x:Type TextBox}}" TargetType="TextBox">
                            <Setter Property="Margin" Value="2" />
                            <Setter Property="MinWidth" Value="100" />
                            <Setter Property="Height" Value="28" />
                        </Style>
                    </autogrid:AutoGrid.Resources>

                    <TextBlock Text="刷怪触发字段" />
                    <TextBox Text="{Binding RefreshMonTrigger}" />

                    <TextBlock Text="清怪触发字段" />
                    <TextBox Text="{Binding ClearMonTrigger}" />

                    <Button Command="{Binding GenerateRefreshMonScriptCommand}" Content="生成动态刷怪脚本" />
                    <Button Command="{Binding ClearRefreshMonScriptCommand}" Content="清除动态刷怪脚本" />
                </autogrid:AutoGrid>

                <GroupBox
                    Width="320"
                    Height="300"
                    Margin="2,10,2,0"
                    Header="超出上限的地图（生成前总数）">
                    <DataGrid
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        CanUserResizeRows="False"
                        CanUserSortColumns="True"
                        ClipboardCopyMode="ExcludeHeader"
                        GridLinesVisibility="Horizontal"
                        IsReadOnly="True"
                        ItemsSource="{Binding GenerationResults}"
                        SelectionMode="Extended"
                        SelectionUnit="Cell">
                        <DataGrid.Resources>
                            <Style x:Key="CopyableResultTextStyle" TargetType="TextBox">
                                <Setter Property="Padding" Value="4,0" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="Cursor" Value="IBeam" />
                                <Setter Property="IsReadOnly" Value="True" />
                                <Setter Property="IsReadOnlyCaretVisible" Value="True" />
                                <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=DataGridRow}}" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Foreground">
                                    <Setter.Value>
                                        <MultiBinding Converter="{StaticResource MonsterCountToRedBrushConverter}">
                                            <Binding Path="MonsterCount" />
                                            <Binding Path="DataContext.MaxMonstersPerMap" RelativeSource="{RelativeSource AncestorType=UserControl}" />
                                        </MultiBinding>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn
                                Width="75"
                                ClipboardContentBinding="{Binding MapCode}"
                                Header="地图代码"
                                SortMemberPath="MapCode">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource CopyableResultTextStyle}" Text="{Binding MapCode, Mode=OneWay}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn
                                Width="*"
                                ClipboardContentBinding="{Binding MapName}"
                                Header="地图名称"
                                SortMemberPath="MapName">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource CopyableResultTextStyle}" Text="{Binding MapName, Mode=OneWay}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn
                                Width="100"
                                ClipboardContentBinding="{Binding MonsterCount}"
                                Header="怪物刷新总数"
                                SortMemberPath="MonsterCount">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBox Style="{StaticResource CopyableResultTextStyle}" Text="{Binding MonsterCount, Mode=OneWay}" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>
            </StackPanel>

        </StackPanel>

    </Grid>
</UserControl>
